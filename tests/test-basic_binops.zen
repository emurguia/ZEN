int main()
{

	float x;
	float y;
	float sum;
	float product;
	float quotient;
	float diff;

	x = 2.75;
	y = 1.1;

	sum = x+y;
	product = x*y;
	quotient = x/y;
	diff = x-y;

	printf(sum);
	printf(product);
	printf(quotient);
	printf(diff);
	return 0;
}
