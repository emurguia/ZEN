void main()
{
       	make_window();
        while(keep_open())
        {
        
                make_triangle(100,450,50,50);
                render();
        }
        close_window();
        print("Success");
	return;
}
