int main()
{
	int x;
	int y;

	bool less;
	bool more;
	bool equal;
	bool leq;
	bool geq;
	bool neq;

	x = 5;
	y = 3;

	less = x<y;
	more = x>y;
	equal = x==y;
	leq = x<=x;
	geq = y>=x;
	neq = x!=y;

	printb(less);
	printb(more);
	printb(equal);
	printb(leq);
	printb(geq);
	printb(neq);

	return 0;
}
